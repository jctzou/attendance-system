# 員工打卡系統 - 雲端架構實作指南

## 🎯 新技術棧

| 功能 | 技術選擇 | 說明 |
|------|---------|------|
| **資料庫** | Supabase (PostgreSQL) | 免費 500MB，包含即時訂閱、Row Level Security |
| **後端 API** | Supabase API | 自動生成 RESTful API，無需手寫後端 |
| **認證** | Supabase Auth | 內建登入、JWT、記住登入功能 |
| **前端框架** | Next.js 14 (App Router) | React 框架，支援 SSR、檔案路由 |
| **UI 樣式** | Tailwind CSS | 快速開發美觀介面 |
| **部署** | Vercel | 免費部署，自動 CI/CD |
| **版本控制** | GitHub | 程式碼管理 |

---

## ✅ 為什麼這個方案更好？

### VS 本地 PHP + MySQL

| 項目 | 本地方案 | 雲端方案 |
|------|---------|---------|
| 環境設定 | 需安裝 PHP、MySQL | **無需安裝，註冊即用** |
| 資料庫管理 | 手動 SQL | **圖形化介面** |
| 後端開發 | 需寫所有 API | **自動生成 API** |
| 權限控制 | 手動實作 | **內建 RLS** |
| 部署 | 需租伺服器 | **免費一鍵部署** |
| HTTPS | 需設定 SSL | **自動 HTTPS** |
| 擴展性 | 單機限制 | **自動擴展** |

---

## 📋 實作步驟總覽

### Phase 1: 設定雲端服務（30 分鐘）
1. 建立 Supabase 專案
2. 建立 GitHub 儲存庫
3. 連接 Vercel

### Phase 2: 資料庫設計（1 小時）
1. 在 Supabase 建立資料表
2. 設定 Row Level Security (RLS)
3. 建立測試資料

### Phase 3: 前端開發（1-2 週）
1. 建立 Next.js 專案
2. 實作登入功能
3. 實作打卡介面
4. 實作管理功能

### Phase 4: 部署上線（10 分鐘）
1. Push 到 GitHub
2. Vercel 自動部署
3. 測試線上功能

---

## 🚀 開始：Phase 1 - 設定雲端服務

### 步驟 1：建立 Supabase 專案

#### 1.1 註冊 Supabase
1. 前往：https://supabase.com
2. 點擊「Start your project」
3. 使用 GitHub 帳號登入（推薦）或 email 註冊

#### 1.2 建立新專案
1. 點擊「New project」
2. 填寫資訊：
   - **Name**: `attendance-system`（員工打卡系統）
   - **Database Password**: 設定一個強密碼（自動生成即可）
   - **Region**: 選擇「Northeast Asia (Tokyo)」（最接近台灣）
   - **Pricing Plan**: 選擇「Free」
3. 點擊「Create new project」
4. 等待 2-3 分鐘建立完成

#### 1.3 取得連線資訊
專案建立完成後：
1. 點選左側「Project Settings」
2. 點選「API」
3. 記下以下資訊（等等會用到）：
   - **Project URL**: `https://xxxxx.supabase.co`
   - **anon public key**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`

---

### 步驟 2：建立 GitHub 儲存庫

#### 2.1 建立新 Repository
1. 前往：https://github.com
2. 點擊右上角「+」→「New repository」
3. 填寫：
   - **Repository name**: `attendance-system`
   - **Description**: 員工打卡及薪資管理系統
   - **Public** 或 **Private**（建議 Private）
   - ✅ 勾選「Add a README file」
4. 點擊「Create repository」

#### 2.2 Clone 到本地
開啟終端機（即使 Homebrew 沒裝好也可以用）：

```bash
cd ~/Desktop/AntigravityTest
git clone https://github.com/你的使用者名稱/attendance-system.git
cd attendance-system
```

---

### 步驟 3：連接 Vercel

#### 3.1 註冊 Vercel
1. 前往：https://vercel.com
2. 點擊「Sign Up」
3. 選擇「Continue with GitHub」
4. 授權 Vercel 存取您的 GitHub

#### 3.2 匯入專案（稍後執行）
等我們建立好 Next.js 專案後再匯入。

---

## 🗄️ Phase 2: 資料庫設計

### 步驟 4：在 Supabase 建立資料表

#### 4.1 使用 SQL Editor
1. 在 Supabase Dashboard，點選左側「SQL Editor」
2. 點擊「New query」

#### 4.2 執行建表腳本

我會為您準備完整的 PostgreSQL 建表腳本（從 MySQL 轉換）。

主要差異：
- `INT AUTO_INCREMENT` → `SERIAL` 或 `BIGSERIAL`
- `TINYINT(1)` → `BOOLEAN`
- `ENUM` → `TEXT CHECK (value IN (...))`
- `DATETIME` → `TIMESTAMPTZ`

---

## 💻 Phase 3: Next.js 專案開發

### 步驟 5：建立 Next.js 專案

#### 5.1 檢查是否有 Node.js

```bash
node -v
```

如果沒有，安裝 Node.js：
1. 前往：https://nodejs.org
2. 下載「LTS」版本
3. 安裝後重新開啟終端機

#### 5.2 建立 Next.js 專案

```bash
cd ~/Desktop/AntigravityTest/attendance-system
npx create-next-app@latest . --typescript --tailwind --app --no-src-dir
```

選項說明（安裝時會詢問）：
- TypeScript: **Yes**
- ESLint: **Yes**
- Tailwind CSS: **Yes**
- `src/` directory: **No**
- App Router: **Yes**
- Customize default import alias: **No**

#### 5.3 安裝 Supabase SDK

```bash
npm install @supabase/supabase-js @supabase/auth-helpers-nextjs
```

#### 5.4 設定環境變數

建立 `.env.local` 檔案：

```env
NEXT_PUBLIC_SUPABASE_URL=你的專案URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=你的anon key
```

---

## 📦 專案架構

```
attendance-system/
├── app/
│   ├── (auth)/
│   │   ├── login/
│   │   │   └── page.tsx          # 登入頁面
│   │   └── layout.tsx             # 認證布局
│   ├── (dashboard)/
│   │   ├── dashboard/
│   │   │   └── page.tsx          # 員工儀表板
│   │   ├── attendance/
│   │   │   └── page.tsx          # 打卡記錄
│   │   ├── leaves/
│   │   │   └── page.tsx          # 請假管理
│   │   ├── admin/
│   │   │   ├── users/
│   │   │   │   └── page.tsx      # 人員管理
│   │   │   ├── salary/
│   │   │   │   └── page.tsx      # 薪資試算
│   │   │   └── reports/
│   │   │       └── page.tsx      # 統計報表
│   │   └── layout.tsx             # Dashboard 布局
│   ├── api/
│   │   └── auth/
│   │       └── [...nextauth]/
│   │           └── route.ts       # 認證 API
│   ├── layout.tsx                 # 根布局
│   └── page.tsx                   # 首頁
├── components/
│   ├── ui/                        # UI 元件
│   ├── attendance/                # 打卡相關元件
│   └── admin/                     # 管理相關元件
├── lib/
│   ├── supabase/
│   │   ├── client.ts             # Supabase Client
│   │   └── server.ts             # Supabase Server
│   └── utils.ts                   # 工具函式
├── types/
│   └── database.ts                # TypeScript 型別定義
├── .env.local                     # 環境變數
├── next.config.js
├── package.json
└── tailwind.config.js
```

---

## 🚢 Phase 4: 部署到 Vercel

### 步驟 6：部署

#### 6.1 Push 到 GitHub

```bash
git add .
git commit -m "Initial commit"
git push origin main
```

#### 6.2 在 Vercel 匯入專案

1. 登入 Vercel：https://vercel.com
2. 點擊「Add New...」→「Project」
3. 選擇您的 `attendance-system` 儲存庫
4. 點擊「Import」
5. 設定環境變數：
   - `NEXT_PUBLIC_SUPABASE_URL`: 貼上您的 URL
   - `NEXT_PUBLIC_SUPABASE_ANON_KEY`: 貼上您的 key
6. 點擊「Deploy」

#### 6.3 完成！

部署完成後，您會得到一個網址，例如：
```
https://attendance-system.vercel.app
```

**之後每次 push 到 GitHub，Vercel 會自動重新部署！**

---

## 🎓 學習資源

- **Supabase 文件**: https://supabase.com/docs
- **Next.js 文件**: https://nextjs.org/docs
- **Tailwind CSS**: https://tailwindcss.com/docs
- **Vercel 文件**: https://vercel.com/docs

---

## 📊 成本估算（完全免費方案）

| 服務 | 免費額度 | 足夠用量 |
|------|---------|---------|
| **Supabase** | 500MB 資料庫<br>50,000 月活躍用戶 | ✅ 100 人公司綽綽有餘 |
| **Vercel** | 100GB 頻寬/月<br>無限網站 | ✅ 中小企業夠用 |
| **GitHub** | 無限儲存庫（Private） | ✅ 完全夠用 |

---

## ⚡ 下一步

請告訴我您想要：

### 選項 A：我來協助您逐步設定
- 我會逐步引導您建立 Supabase、GitHub、Vercel
- 我會幫您建立完整的資料表腳本
- 我會建立 Next.js 專案基礎架構

### 選項 B：我先自己試試看
- 我提供完整文件讓您自己操作
- 遇到問題隨時問我

**建議選 A，我會陪您一步步完成設定！**
